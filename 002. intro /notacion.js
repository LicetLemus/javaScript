//Notacion 
// ; -> delimitar el final de uuna linea, actualmente casi no se usa 
const b = 4;
b + 4

/* 
. -> se utiliza en los objetos para acceder a los atributos 
un movil tiene difernes atributos (altura, anchura, profundidad)
accedo -- movil.anchura
*/

// [] -> se utilizan para las listas, arreglos o arrays, tambien definir un valor del array ar[]
const ar = [1, 2, 3, 4]
console.log(ar[2])

// () -> se utilizan para funciones

function suma(a, b) {
    //se escribe la funcion
}

// {} -> se utilizan para objetos, funciones y estructuras de control 

const movil = {
    anchura: 5,
    altura: 10
}

if(true) {
    //todo lo que haya ebtre llaves
    const constante2 = "hola"
}

